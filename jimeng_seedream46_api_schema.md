# 即梦AI 图片生成 4.6 —— 接口 Schema

> 来源：火山引擎文档 <https://docs.volcengine.com/docs/85621/2275082?lang=zh>
> 模型：即梦图片 4.6（基于 Seedream4.0 基础模型训练，聚焦大众修图场景：人像写真、平面设计、图片风格化）

## 0. 通用接入信息

| 项 | 值 |
|---|---|
| 接口地址 | `https://visual.volcengineapi.com` |
| 请求方式 | `POST` |
| Content-Type | `application/json` |
| 鉴权 | Region 固定 `cn-north-1`，Service 固定 `cv`（签名参数见公共文档） |

### 限制条件
- 输入图：仅支持 JPEG / PNG（建议 JPEG），单文件 ≤ 15MB，最多 14 张
- 输入图分辨率：最大 `4096 × 4096`
- 输出图数量：最大 = 15 − 输入图数量，建议 ≤ 6 张
- 输出图以列表返回；prompt 默认按意图判断输出张数（计费按张数）

---

## 1. 提交任务 —— `Action=CVSync2AsyncSubmitTask` & `Version=2022-08-31`

### 1.1 Query 参数

| 参数 | 类型 | 必选 | 说明 |
|---|---|---|---|
| Action | string | 是 | 固定值 `CVSync2AsyncSubmitTask` |
| Version | string | 是 | 固定值 `2022-08-31` |

### 1.2 Body 参数

| 名称 | 类型 | 必选 | 描述 |
|---|---|---|---|
| req_key | string | 是 | 服务标识，固定值 `jimeng_seedream46_cvtob` |
| image_urls | array&lt;string&gt; | 否 | 图片 URL 列表，0~14 张，建议 ≤ 6 张（过多参考效果降低） |
| prompt | string | 是 | 提示词，中英文均可，≤ 800 字符（过长可能出图异常或不生效），不建议输入 `$` 等特殊符号 |
| size | int | 否 | 生成图面积。默认 `4194304`（即 2048×2048，2K）。取值范围 `[1024×1024, 4096×4096]`。面积/宽高二选一，同时传则优先宽高；只传面积时模型按 prompt 意图智能判断宽高比 |
| width | int | 否 | 生成图宽度。需与 height 同时传才生效。宽高乘积在 `[1024×1024, 4096×4096]`，宽高比在 `[min_ratio, max_ratio]` 之间 |
| height | int | 否 | 生成图高度（与 width 成对） |
| scale | int | 否 | 文本描述影响程度。默认 `50`，范围 `[1, 100]`；越大文本影响越大、输入图影响越小 |
| force_single | bool | 否 | 是否强制单图输出。默认 `false`。对延迟/价格敏感建议置 `true` |
| min_ratio | float | 否 | 结果宽/高下限。默认 `1/3`，范围 `[1/16, 16)` |
| max_ratio | float | 否 | 结果宽/高上限。默认 `3`，范围 `(1/16, 16]` |
| callback_url | string | 否 | 回调接口 URL（需公网可访问），仅异步回调生效 |
| return_url | bool | 否 | 回调时图片资源以链接返回（24h 有效）。默认 `false` |
| logo_info | JSON string | 否 | 水印配置，见 1.3（仅异步回调生效） |
| aigc_meta | JSON string | 否 | 隐式标识，见 1.4（仅异步回调生效） |

**推荐宽高参考**（1K / 2K / 4K 各档位）：
- 1K：1024×1024 (1:1)
- 2K：2048×2048 (1:1) / 2304×1728 (4:3) / 2496×1664 (3:2) / 2560×1440 (16:9) / 3024×1296 (21:9)
- 4K：4096×4096 (1:1) / 4693×3520 (4:3) / 4992×3328 (3:2) / 5404×3040 (16:9) / 6197×2656 (21:9)

### 1.3 LogoInfo（序列化后赋给 `logo_info`）

| 名称 | 类型 | 必选 | 描述 |
|---|---|---|---|
| add_logo | bool | 否 | 是否添加水印，默认 `false` |
| position | int | 否 | 位置：0 右下 / 1 左下 / 2 左上 / 3 右上，默认 `0` |
| language | int | 否 | 语言：0 中文「AI生成」/ 1 英文「Generated by AI」，默认 `0` |
| opacity | float | 否 | 不透明度 0~1，默认 `1` |
| logo_text_content | string | 否 | 明水印自定义内容 |

### 1.4 AIGCMeta（序列化后赋给 `aigc_meta`，依据《人工智能生成合成内容标识办法》）

| 名称 | 类型 | 必选 | 描述 |
|---|---|---|---|
| content_producer | string | 否 | 内容生成服务 ID |
| producer_id | string | 是 | 生成服务商给此图片的唯一 ID |
| content_propagator | string | 否 | 内容传播服务商 ID |
| propagate_id | string | 否 | 传播服务商给此图片的唯一 ID |

### 1.5 返回参数（`data` 重点关注）

| 字段 | 类型 | 说明 |
|---|---|---|
| task_id | string | 任务 ID，用于查询结果 |

请求示例：
```json
{
    "req_key": "jimeng_seedream46_cvtob",
    "image_urls": ["https://xxxx"],
    "prompt": "千军万马"
}
```
返回示例：
```json
{
    "code": 10000,
    "data": { "task_id": "7392616336519610409" },
    "message": "Success",
    "request_id": "20240720103939AF0029465CF6A74E51EC",
    "time_elapsed": "104.852309ms"
}
```

---

## 2. 查询任务 —— `Action=CVSync2AsyncGetResult` & `Version=2022-08-31`

### 2.1 Query 参数

| 参数 | 类型 | 必选 | 说明 |
|---|---|---|---|
| Action | string | 是 | 固定值 `CVSync2AsyncGetResult` |
| Version | string | 是 | 固定值 `2022-08-31` |

### 2.2 Body 参数

| 名称 | 类型 | 必选 | 描述 |
|---|---|---|---|
| req_key | string | 是 | 固定值 `jimeng_seedream46_cvtob` |
| task_id | string | 是 | 提交任务接口返回的任务 ID |
| req_json | JSON string | 否 | 序列化后的配置字符串，支持 `return_url` / `logo_info` / `aigc_meta`（结构与提交接口一致，见 1.3 / 1.4） |

### 2.3 返回参数（`data` 重点关注）

| 字段 | 类型 | 说明 |
|---|---|---|
| binary_data_base64 | array&lt;string&gt; | 返回图片的 base64 数组 |
| image_urls | array&lt;string&gt; | 图片 URL 数组，输出格式 PNG（链接 24h 有效） |
| status | string | `in_queue` 已提交 / `generating` 处理中 / `done` 完成（按外层 code & message 判断成败）/ `not_found` 无此任务或已过期(12h) / `expired` 已过期需重新提交 |

返回示例：
```json
{
    "code": 10000,
    "data": {
        "binary_data_base64": null,
        "image_urls": ["https://xxxx"],
        "status": "done"
    },
    "message": "Success",
    "status": 10000,
    "request_id": "2025061718460554C9B78D23B0BAB45B2A",
    "time_elapsed": "508.312154ms"
}
```
报错示例（`code != 10000` 时 `data` 为 `null`）：
```json
{
    "code": 50413,
    "data": null,
    "message": "Post Text Risk Not Pass",
    "request_id": "202511281418218670D408837A9B0EB58F",
    "status": 50413,
    "time_elapsed": "36.799829ms"
}
```

---

## 3. 业务错误码

| HttpCode | 错误码 | 错误消息 | 描述 | 是否重试 |
|---|---|---|---|---|
| 200 | 10000 | 无 | 请求成功 | 不需要 |
| 400 | 50411 | Pre Img Risk Not Pass | 输入图片前审核未通过 | 不需要 |
| 400 | 50511 | Post Img Risk Not Pass | 输出图片后审核未通过 | 可重试 |
| 400 | 50412 | Text Risk Not Pass | 输入文本前审核未通过 | 不需要 |
| 400 | 50512 | Post Text Risk Not Pass | 输出文本后审核未通过 | 不需要 |
| 400 | 50413 | Post Text Risk Not Pass | 输入文本含敏感词、版权词等审核不通过 | 不需要 |
| 400 | 50518 | Pre Img Risk Not Pass: Copyright | 输入版权图前审核未通过 | 不需要 |
| 400 | 50519 | Post Img Risk Not Pass: Copyright | 输出版权图后审核未通过 | 可重试 |
| 400 | 50520 | Risk Internal Error | 审核服务异常 | 不需要 |
| 400 | 50521 | Antidirt Internal Error | 版权词服务异常 | 不需要 |
| 400 | 50522 | Image Copyright Internal Error | 版权图服务异常 | 不需要 |
| 429 | 50429 | Request Has Reached API Limit, Please Try Later | QPS 超限 | 可重试 |
| 429 | 50430 | Request Has Reached API Concurrent Limit, Please Try Later | 并发超限 | 可重试 |
| 500 | 50500 | Internal Error | 内部错误 | 不需要 |
| 500 | 50501 | Internal RPC Error | 内部算法错误 | 不需要 |

> 判断逻辑建议：优先判断 `code == 10000`，再判断 `data.status`（文档提示顺序反了可能解析 panic）。

---

## 4. 快速参考（最小调用）

```
POST https://visual.volcengineapi.com?Action=CVSync2AsyncSubmitTask&Version=2022-08-31
Content-Type: application/json

{
  "req_key": "jimeng_seedream46_cvtob",
  "image_urls": ["https://.../portrait.png"],
  "prompt": "背景换成白色",
  "width": 1024,
  "height": 1024,
  "force_single": true
}
→ data.task_id

POST https://visual.volcengineapi.com?Action=CVSync2AsyncGetResult&Version=2022-08-31
{
  "req_key": "jimeng_seedream46_cvtob",
  "task_id": "<task_id>"
}
→ data.image_urls[] / data.binary_data_base64[]，data.status == "done"
```

> 附注：页面还提供了 Python / Go / Java / PHP 的提交+查询 SDK 示例（`volcengine.visual.VisualService` 的 `cv_sync2async_submit_task` 等方法），需要的话我可以再提取。
