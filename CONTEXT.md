# FramedPhoto

把一块低功耗彩色电子纸变成家庭信息窗口：照片回忆、天气日历和 AI 生成的
图文卡片按时段轮换，设备拉取显示后深度休眠。

## Language

### 内容资产

**内容库 (Content Library)**:
用户主动上传的图片集合；入库默认不上屏，须「推送到显示」才参与显示。
_Avoid_: 图库、相册、上传目录

**照片库 (Photo Library)**:
私人照片集合，是每日精选唯一的选片池。NAS `/volume1/photo` 是唯一共享
照片目录与最终存储（ADR-0007）：rsync 增量同步到本地镜像供分析选片，
管理台上传的照片反向推回 NAS。不按人建夹，全家共用。
_Avoid_: 相册、图库、每人一夹

**照片分类 (Photo Category)**:
照片库的二级划分：photo 第一层文件夹名，一张照片恰好属于一个分类，
不可手动指派或一照多挂；文件夹可在管理台新建 / 重命名 / 删除空文件夹
（NAS 先行、镜像与评分随迁，ADR-0007）。时段绑定分类后选片严格限于
该分类。
_Avoid_: tag、标签、相册、合集、Frame_名字

**未分类 (Uncategorized)**:
photo 根目录散照 / 内容库上传图所属的照片分类；正常参与选片。
_Avoid_: 默认分类、根目录

**原图 (Original)**:
某内容在量化为 FPS6 帧之前的图像；内容库的 .orig、照片库的照片文件、
天气卡片与自由模块生成时落盘的中间图。原图与 FPS6 帧同生同灭：删除原图时
帧一并删除。
_Avoid_: 源图、底图、母图

### 管理台

**部署前缀 (Deploy Base)**:
管理台页面被前缀代理部署时（如 /apps/frame/）的 URL 前缀；
api() 从自身 script src 推导，代理层不做 JS 内容重写。
_Avoid_: base path、上下文路径

**预览 (Preview)**:
管理台查看某内容图像的功能；永远由原图缩放而来。
不存在由 FPS6 帧还原出的预览。
_Avoid_: 回退预览（已废除）、真机观感预览、效果预览

### 照片时段的内容决策

**每日精选 (Daily Pick)**:
每个照片时段当日各自自动选出的照片及其回忆文案；多个照片时段
（即使绑同一照片分类）各自独立选片。
_Avoid_: 每日照片、今日照片、今日精选

**历史上的今天 (On This Day)**:
每日精选的首选选片策略：照片拍摄日期的月-日与今日相同。
_Avoid_: 同日照片、周年照片

**手动精选 (Manual Pick)**:
用户指定的当日精选，全局优先于一切照片时段的自动选片，
无论该照片属于哪个照片分类。
_Avoid_: 今日精选、手动指定

**回忆度 (Memory Score)**:
照片唤起回忆的价值评分，每日精选选片的主依据。
_Avoid_: 评分、AI 分数

**美观度 (Beauty Score)**:
照片画面美感的评分，降级选片时的排序依据。

### 生成内容

**自由模块 (Free Module)**:
由主题 prompt 驱动的图文卡片生成器：LLM 生成文案、文生图生成插画，
每天为所在时段生成一张卡片。
_Avoid_: 新闻卡片（旧称）、模块（单用易歧义）

**天气卡片 (Weather Card)**:
按定位城市渲染的天气与日历卡片。

**日历卡片 (Calendar Card)**:
一页当月月历 + 每日家庭寄语的纯本地卡片：公历 + 农历 + 节气/节日
（节日红 / 节气绿 / 农历黑），今日淡黄底格 + 红圈，周日红周六蓝，
六色全用；寄语 LLM 每日生成，失败降级内置语料轮换。无网络依赖，恒可用。
_Avoid_: 月历模块、万年历、日历时段（那是内容类型层面）

### 编排与分发

**时段 (Slot)**:
24 小时线性分段中的一段，绑定一种内容类型，支持跨午夜。
_Avoid_: 时间块、时隙、时段配置

**内容类型 (Slot Type)**:
时段绑定的四种内容之一：天气、日历、照片、自由模块。
_Avoid_: 内容模式

**内容源 (Content Source)**:
一种内容的渲染命名空间：id 前缀标识归属，独占该 id 到 FPS6 帧与
原图的渲染知识。共六种：内容库、每日精选、置顶显示、天气卡片、
日历卡片、自由模块；不决定谁上屏（谁上屏由置顶显示 > 推送到显示 >
时段的优先级决定）。
_Avoid_: 内容类型（那是时段层的）、来源

**内容清单 (Content Manifest)**:
设备轮询服务端获得的当前应显示内容。
_Avoid_: 内容列表、图片列表

**内容指纹 (Content Fingerprint)**:
生成源条目 id 的语义：由渲染字节哈希而来（前缀-哈希前 10 位），
仅供设备变更检测，不是查找键。内容库 id 例外：是查找键。
_Avoid_: 把生成源 id 当查找键用

**推送到显示 (Content Push)**:
把内容库的一张图片指定为当日照片时段的优先显示内容，当日有效。
_Avoid_: 上屏、推送（单用）、置顶（易与置顶显示混淆）

**置顶显示 (Pinned Display)**:
一次性指定立即上屏的内容，优先于一切时段内容。
_Avoid_: 临时显示、插播、display（单用）

### 屏幕显色

**墨水对混色 (Ink-Pair Mixing)**:
色域外高饱和色量化为两种墨水按最小二乘比例抖动混合的策略
（如纯青 → 白+蓝）；色域内颜色仍走 6 墨涌现式混色。见 ADR-0004。
_Avoid_: 双色混色、混色算法（单用）

**浓彩偏置 (Chroma Bias)**:
墨水对混色向彩色端偏移的强度（0..4）；0 为忠实混色，越大越浓彩（偏暗）。
管理台校准页滑杆，存 runtime_config。
_Avoid_: 饱和度增益、色彩增强

### 设备侧

**刷屏 (Screen Refresh)**:
把内容写入电子纸屏面的动作；内容未变则不刷屏。
_Avoid_: 刷新、重绘

**心跳 (Heartbeat)**:
设备周期性向服务端上报状态；轮询内容清单时可附带完成，响应携带的服务器
时间是设备墙钟的权威来源。
_Avoid_: 状态上报、保活

**对齐唤醒 (Aligned Wake)**:
设备的休眠节拍：每次心跳校准墙钟后，深度休眠到下一个本地半点边界加一分钟
（每个整点的 ：01 / :31）再唤醒；墙钟未校准时按固定间隔兑底。
_Avoid_: 定时唤醒、整点唤醒（是 ：01/:31，不是 ：00）

**OTA**:
设备无线更新固件的机制，更新失败自动回滚到旧版本。
_Avoid_: 固件升级、远程升级
