# 时段选片严格限于所绑分类，不出圈

照片时段绑定照片分类后，当日选片严格限于该分类内，绝不静默回退全库。绑定即主题，出圈（哪怕是「保证有图可显」的降级）都背叛用户的编排意图。

## Consequences

- 分类内三级降级：月-日匹配+门槛 → 分类内回忆度 → 分类内任意；只要分类里有照片就一定上屏。
- 分类为空（0 张）的时段跳过、设备保持原屏、管理台警告——这是刻意行为而非 bug，不要「修复」成回退全库。
- 未绑定分类的照片时段选片池为全库（含未分类）。
- 手动精选例外：全局生效，顶掉一切照片时段的自动选片，无论照片属于哪个分类。
